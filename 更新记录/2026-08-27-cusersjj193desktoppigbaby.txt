══════════════════════════════════════════════
PigBaby 开发记录 · 2026-08-27（深色会话摘录）
══════════════════════════════════════════════

【本次修复】用户反馈两个问题：

1. 任务设置了提醒时间但到点没有收到本地通知
   - 排查发现两处隐患：
     a) 通知 id 由任务 id 哈希生成，可能超过 int 上限，
        插件 cancel/update 用 getInt() 解析会抛异常，导致
        resync 时先取消再重排的流程中断，提醒整体失效。
        → notifIdForTask 改为 (h & 0x7fffffff) || 1，保证正 int。
     b) 调度失败（通知权限被拒 / 精确闹钟未授权降级为不精确）
        被静默吞掉，用户毫无感知。
        → scheduleTaskReminder 现在返回状态
          （scheduled / inexact / permission-needed / failed），
          Tasks 页在保存带提醒的任务后弹窗提示：
          · 未开通知权限 → 引导允许
          · 精确闹钟未授权 → 提示到"设置→应用→PigBaby→闹钟和提醒"开启
            并调用 changeExactNotificationSetting 直达该设置页
   - 注意：Android 12+ 首次设置提醒时，插件会自动弹出
     "闹钟和提醒"设置页，若用户直接退出，提醒降级为不精确闹钟，
     可能延迟；国产 ROM 上 App 被划掉后台也可能不响，
     需要开"自启动"。

2. 点击"授权短信"直接提示失败，系统权限弹窗不出现
   - 根因：@CapacitorPlugin 的权限 launcher 机制在设备上
     请求 READ_SMS 时未注册回调即被拒（无弹窗、立即 reject）。
   - 修复：SmsReaderPlugin 改用 legacy 权限流程：
     @NativePlugin(permissionRequestCode=7711) +
     ActivityCompat.requestPermissions 直接发起请求 +
     handleRequestPermissionsResult 回传结果，确定性分发。
   - SmsAutoPanel 顺带修复：toggle 开关闭包读到旧 permission
     导致授权成功后仍无法开启；失败时展示真实错误消息。

【其他】
- 合并上一提交 ff95acc（已过期任务与当天任务统一排序）
- 构建：npm run build + npx cap sync android + gradlew assembleDebug 均通过
- APK：android/app/build/outputs/apk/debug/app-debug.apk

【追加修复】排序跨类型无效：
- 现象：今天视图里点 ▲▼ 把任务移到"过期任务/今天任务"两组的交界处，顺序不变。
- 根因：todayViewTasks 是 [过期组, 今天组] 两个数组各自按 order 排序后再拼接；
  跨组交换只改了 order，但渲染时每组重新按自己的 order 排序，
  交换被抵消，视觉上毫无变化。
- 修复：过期未完成 + 今天未完成合并为"一个整体"按 order 排序渲染，
  order 是唯一顺序依据，跨组移动立即生效；过期任务仍保留日期徽标。
- 提交：后续 commit（在 5f011f2 之后）

【追加更新】个人模块 + 支出备注记忆 + 按日查询柱状图：
1. 右上角新增"👤 个人"入口（固定悬浮按钮，App 级），新页面 /profile：
   - 权限设置：短信自动记账面板整体迁入（授权/开关/导入）；
     另加通知权限状态与申请按钮（任务提醒用）
   - 个人中心占位区（头像昵称/主题/云同步，为后续个性化留空间）
   - 实现：SmsAutoPanel 拆分为 UI 版（Profile 用，自读存储）
     与 SmsAutoSync 隐形轮询引擎（仍挂记账页，保持自动导入不停）
2. 支出备注记忆：记支出时若备注非空，按 标签→备注→次数 存入
   （Preferences pigbaby_tag_notes，含备份导入导出）。再次记同一
   标签支出时，标签下方显示"常用备注"列表，按使用次数降序，
   点击填入备注，每条可 ✕ 删除。
3. 按日查询：当日支出按标签聚合为柱状图（复用 tag-bar 样式，
   金额降序，显示合计）；点击某标签后下方记录列表只显示该标签
   的支出明细，可"显示全部"返回；切换日期自动清除过滤。
- 文件：App.tsx/App.css、Profile.tsx/Profile.css（新）、
  SmsAutoSync.tsx（新）、SmsAutoPanel.tsx、storage.ts、
  AddRecordModal.tsx/css、DailyRecords.tsx、Accounting.css

【追加更新】版本号 1.3.0 + 熄屏通知修复：
- 版本：package.json 1.2.0→1.3.0；android versionCode 2→3、
  versionName 1.2→1.3.0（aapt 已验证）；个人页显示版本号
- 熄屏收不到通知的修复措施：
  1) 新建高优先级通知通道 pigbaby_reminders（importance HIGH，
     插件的默认通道只是 DEFAULT，熄屏/锁屏表现弱），提醒统一
     走该通道
  2) schedule 后校验系统是否真的接受了调度
     （res.notifications 包含该 id），未接受则明确报"系统未接受
     本次提醒调度"，不再静默
  3) 个人页新增"⏰ 精确闹钟（熄屏准点提醒）"权限行：显示
     已开启/未开启，可一键跳到系统"闹钟和提醒"设置页
  4) 提醒降级为不精确时的弹窗文案明确提示"熄屏时可能收不到"
- 说明：Android 12 必须开启"闹钟和提醒"特殊权限，否则降级为
  不精确闹钟（熄屏/后台时延迟，进入 App 才触发——用户看到的
  现象）；Android 13+ 由 USE_EXACT_ALARM 自动授予。若权限已开
  仍收不到，多为国产 ROM 省电策略/锁屏通知设置问题。

【追加修复】v1.3.1 提醒无声音/震动 + 熄屏显示：
- 根因：上一版用 JS createChannel 建的 pigbaby_reminders 通道，
  插件实现里 sound 默认 null（静音）、vibrate 默认 false（无震动）、
  useLights 默认 false，且 JS 端无法引用系统默认提示音
  （sound 字段只会拼 android.resource://pkg/raw/ 资源路径）
- 修复：改在 MainActivity 原生创建通道
  （IMPORTANCE_HIGH + Settings.System.DEFAULT_NOTIFICATION_URI
  系统默认提示音 + 震动 + 锁屏 VISIBILITY_PUBLIC），JS 侧不再
  createChannel（避免覆盖原生配置），只用 channelId 引用
- 熄屏无消息：通知实际已送达（解锁后可见），剩余原因在
  系统"锁屏通知"设置（国产 ROM 每 App 单独开关）或省电策略，
  需用户侧确认；通道已设锁屏可见尽量兜底
- 版本：1.3.0 → 1.3.1（versionCode 4）

【追加更新】v1.4.0 暗色主题系统 + 记账概览美化：
1. 暗色主题"酷黑"（黑色基调，青紫霓虹点缀，简约风格）：
   - index.css 新增 .theme-dark 变量覆盖层（bg/卡片/文字/边框/
     主色全量换色），应用到 document.body
   - 底部猪按钮暗色下改为青紫渐变 + 霓虹光圈
   - 底部导航暗色微光、选中态高亮随主题
   - 修复硬编码浅色：日期备注输入框、撤销 toast、任务过期/
     拖拽状态、CheckIn 选中按钮（新增 --on-primary 文字色 token）、
     标签选中行（--tag-selected）
2. 主题切换：个人→个人中心→界面主题，🌸粉嫩/🖤酷黑 二选一，
   Preferences 持久化（含备份导入导出），即切即生效
3. 记账月概览 UI 精简：
   - 余额卡片压缩（内边距/字号全面缩小）
   - 收入/支出按钮改为紧凑胶囊
   - 最近记录默认只显示 3 条，底部"显示更多"按钮每次 +5 条、
     翻尽后自动加载更早日期，替代原滚动加载哨兵
4. 版本 1.4.0（versionCode 5）

【追加更新】v1.4.1 删导入导出 + 暗色主题调优：
1. 删除记账页"导出/导入"按钮及相关 storage 函数
   （exportAllData/importAllData），为后续线上数据与用户绑定
   铺路；短信"导入最近账单"保留
2. 暗色主题减淡：bg #0A0A0F→#12121A、卡片 #14141C→#1C1C26、
   边框/次要文字同步提亮，不再死黑
3. 余额显示板暗色化：粉色渐变替换为暗色赛博面板——
   青/紫双层光晕 + 细网格纹理 + 霓虹描边 + 金额辉光，
   负值版本为暗红+紫光；明细栏玻璃化
