══════════════════════════════════════════════
PigBaby 开发记录 · 2026-08-27（深色会话摘录）
══════════════════════════════════════════════

【本次修复】用户反馈两个问题：

1. 任务设置了提醒时间但到点没有收到本地通知
   - 排查发现两处隐患：
     a) 通知 id 由任务 id 哈希生成，可能超过 int 上限，
        插件 cancel/update 用 getInt() 解析会抛异常，导致
        resync 时先取消再重排的流程中断，提醒整体失效。
        → notifIdForTask 改为 (h & 0x7fffffff) || 1，保证正 int。
     b) 调度失败（通知权限被拒 / 精确闹钟未授权降级为不精确）
        被静默吞掉，用户毫无感知。
        → scheduleTaskReminder 现在返回状态
          （scheduled / inexact / permission-needed / failed），
          Tasks 页在保存带提醒的任务后弹窗提示：
          · 未开通知权限 → 引导允许
          · 精确闹钟未授权 → 提示到"设置→应用→PigBaby→闹钟和提醒"开启
            并调用 changeExactNotificationSetting 直达该设置页
   - 注意：Android 12+ 首次设置提醒时，插件会自动弹出
     "闹钟和提醒"设置页，若用户直接退出，提醒降级为不精确闹钟，
     可能延迟；国产 ROM 上 App 被划掉后台也可能不响，
     需要开"自启动"。

2. 点击"授权短信"直接提示失败，系统权限弹窗不出现
   - 根因：@CapacitorPlugin 的权限 launcher 机制在设备上
     请求 READ_SMS 时未注册回调即被拒（无弹窗、立即 reject）。
   - 修复：SmsReaderPlugin 改用 legacy 权限流程：
     @NativePlugin(permissionRequestCode=7711) +
     ActivityCompat.requestPermissions 直接发起请求 +
     handleRequestPermissionsResult 回传结果，确定性分发。
   - SmsAutoPanel 顺带修复：toggle 开关闭包读到旧 permission
     导致授权成功后仍无法开启；失败时展示真实错误消息。

【其他】
- 合并上一提交 ff95acc（已过期任务与当天任务统一排序）
- 构建：npm run build + npx cap sync android + gradlew assembleDebug 均通过
- APK：android/app/build/outputs/apk/debug/app-debug.apk

【追加修复】排序跨类型无效：
- 现象：今天视图里点 ▲▼ 把任务移到"过期任务/今天任务"两组的交界处，顺序不变。
- 根因：todayViewTasks 是 [过期组, 今天组] 两个数组各自按 order 排序后再拼接；
  跨组交换只改了 order，但渲染时每组重新按自己的 order 排序，
  交换被抵消，视觉上毫无变化。
- 修复：过期未完成 + 今天未完成合并为"一个整体"按 order 排序渲染，
  order 是唯一顺序依据，跨组移动立即生效；过期任务仍保留日期徽标。
- 提交：后续 commit（在 5f011f2 之后）

【追加更新】个人模块 + 支出备注记忆 + 按日查询柱状图：
1. 右上角新增"👤 个人"入口（固定悬浮按钮，App 级），新页面 /profile：
   - 权限设置：短信自动记账面板整体迁入（授权/开关/导入）；
     另加通知权限状态与申请按钮（任务提醒用）
   - 个人中心占位区（头像昵称/主题/云同步，为后续个性化留空间）
   - 实现：SmsAutoPanel 拆分为 UI 版（Profile 用，自读存储）
     与 SmsAutoSync 隐形轮询引擎（仍挂记账页，保持自动导入不停）
2. 支出备注记忆：记支出时若备注非空，按 标签→备注→次数 存入
   （Preferences pigbaby_tag_notes，含备份导入导出）。再次记同一
   标签支出时，标签下方显示"常用备注"列表，按使用次数降序，
   点击填入备注，每条可 ✕ 删除。
3. 按日查询：当日支出按标签聚合为柱状图（复用 tag-bar 样式，
   金额降序，显示合计）；点击某标签后下方记录列表只显示该标签
   的支出明细，可"显示全部"返回；切换日期自动清除过滤。
- 文件：App.tsx/App.css、Profile.tsx/Profile.css（新）、
  SmsAutoSync.tsx（新）、SmsAutoPanel.tsx、storage.ts、
  AddRecordModal.tsx/css、DailyRecords.tsx、Accounting.css

【追加更新】版本号 1.3.0 + 熄屏通知修复：
- 版本：package.json 1.2.0→1.3.0；android versionCode 2→3、
  versionName 1.2→1.3.0（aapt 已验证）；个人页显示版本号
- 熄屏收不到通知的修复措施：
  1) 新建高优先级通知通道 pigbaby_reminders（importance HIGH，
     插件的默认通道只是 DEFAULT，熄屏/锁屏表现弱），提醒统一
     走该通道
  2) schedule 后校验系统是否真的接受了调度
     （res.notifications 包含该 id），未接受则明确报"系统未接受
     本次提醒调度"，不再静默
  3) 个人页新增"⏰ 精确闹钟（熄屏准点提醒）"权限行：显示
     已开启/未开启，可一键跳到系统"闹钟和提醒"设置页
  4) 提醒降级为不精确时的弹窗文案明确提示"熄屏时可能收不到"
- 说明：Android 12 必须开启"闹钟和提醒"特殊权限，否则降级为
  不精确闹钟（熄屏/后台时延迟，进入 App 才触发——用户看到的
  现象）；Android 13+ 由 USE_EXACT_ALARM 自动授予。若权限已开
  仍收不到，多为国产 ROM 省电策略/锁屏通知设置问题。

【追加修复】v1.3.1 提醒无声音/震动 + 熄屏显示：
- 根因：上一版用 JS createChannel 建的 pigbaby_reminders 通道，
  插件实现里 sound 默认 null（静音）、vibrate 默认 false（无震动）、
  useLights 默认 false，且 JS 端无法引用系统默认提示音
  （sound 字段只会拼 android.resource://pkg/raw/ 资源路径）
- 修复：改在 MainActivity 原生创建通道
  （IMPORTANCE_HIGH + Settings.System.DEFAULT_NOTIFICATION_URI
  系统默认提示音 + 震动 + 锁屏 VISIBILITY_PUBLIC），JS 侧不再
  createChannel（避免覆盖原生配置），只用 channelId 引用
- 熄屏无消息：通知实际已送达（解锁后可见），剩余原因在
  系统"锁屏通知"设置（国产 ROM 每 App 单独开关）或省电策略，
  需用户侧确认；通道已设锁屏可见尽量兜底
- 版本：1.3.0 → 1.3.1（versionCode 4）

【追加更新】v1.4.0 暗色主题系统 + 记账概览美化：
1. 暗色主题"酷黑"（黑色基调，青紫霓虹点缀，简约风格）：
   - index.css 新增 .theme-dark 变量覆盖层（bg/卡片/文字/边框/
     主色全量换色），应用到 document.body
   - 底部猪按钮暗色下改为青紫渐变 + 霓虹光圈
   - 底部导航暗色微光、选中态高亮随主题
   - 修复硬编码浅色：日期备注输入框、撤销 toast、任务过期/
     拖拽状态、CheckIn 选中按钮（新增 --on-primary 文字色 token）、
     标签选中行（--tag-selected）
2. 主题切换：个人→个人中心→界面主题，🌸粉嫩/🖤酷黑 二选一，
   Preferences 持久化（含备份导入导出），即切即生效
3. 记账月概览 UI 精简：
   - 余额卡片压缩（内边距/字号全面缩小）
   - 收入/支出按钮改为紧凑胶囊
   - 最近记录默认只显示 3 条，底部"显示更多"按钮每次 +5 条、
     翻尽后自动加载更早日期，替代原滚动加载哨兵
4. 版本 1.4.0（versionCode 5）

【追加更新】v1.4.1 删导入导出 + 暗色主题调优：
1. 删除记账页"导出/导入"按钮及相关 storage 函数
   （exportAllData/importAllData），为后续线上数据与用户绑定
   铺路；短信"导入最近账单"保留
2. 暗色主题减淡：bg #0A0A0F→#12121A、卡片 #14141C→#1C1C26、
   边框/次要文字同步提亮，不再死黑
3. 余额显示板暗色化：粉色渐变替换为暗色赛博面板——
   青/紫双层光晕 + 细网格纹理 + 霓虹描边 + 金额辉光，
   负值版本为暗红+紫光；明细栏玻璃化

【追加更新】v1.5.0 全屏 + 导航重构 + 任务日历重构：
1. 全屏：styles.xml NoActionBarLaunch 加 windowFullscreen
   （隐藏状态栏铺满屏幕）；#root 去掉 480px 宽度限制
2. 导航：右上角浮动"个人"按钮移除（解决与子导航重叠），
   改为底部导航第 4 个 tab（记账|🐷|任务|个人）；
   记账/任务子导航改紧凑横向滑动条
3. 记账：
   - 暗色下收入/支出按钮同款赛博化（暗渐变+霓虹描边+辉光）
   - 支出记录去掉 💸 图标（概览/按日/标签统计三处），收入保留 💰
   - 自定义标签加 ✕ 删除（角标，确认后删除，账目不受影响）
4. 任务：
   - 主页月历改为"一周条"（周一起始，可翻周/回到今天，含节日
     与任务/备注小点）
   - 新增"日历"子页：完整月历 + 日期备注（从主页迁入）+
     所选日任务列表

【追加更新】v1.5.1 图标精简：
- 记账/任务顶部子导航按钮全部去掉 emoji 图案（纯文字）
- 任务列表标题"今天 的任务"去掉 📅 图案
- 底部导航"个人"图标 👤 换成齿轮 ⚙️

【追加更新】v1.5.2 日历入口 + 全屏加固：
1. 底部导航加"日历"（💰记账 | 📅日历 | 🐷 | 📋任务 | ⚙️个人），
   点击进入任务模块日历视图（/tasks?view=calendar 参数联动，
   离开自动清除）；任务页内子导航移除日历按钮
2. 任务一周条左侧"📅 xx月xx日 起"去掉图案
3. 全屏加固：windowFullscreen 主题属性未生效（Theme.SplashScreen
   忽略），改在 MainActivity 代码层加 FLAG_FULLSCREEN 强制隐藏
   状态栏，应用真正占满手机最上方

【追加更新】v1.5.3 打卡融入任务栏：
1. 打卡目标显示在任务列表中（带"打卡"徽章），与普通任务共用 order
   排序空间，▲▼ 可跨类型移动（CheckInGoal 新增 order 字段）
2. 任务栏勾选打卡目标 → 写入当天打卡记录（success），打卡页同步
   显示已完成；取消勾选/撤销 → 清除当天记录，打卡页同步恢复未完成
3. 打卡任务日期恒为"今天"：当天未完成不会显示过时日期徽章，隔天
   自动刷新为未完成（打卡页同步显示未完成），无需手动重置

【追加更新】v1.5.4 打卡放弃 + 视图修复：
1. 任务栏打卡项增加"放弃"按钮：放弃当天打卡（标记失败 ✗），
   打卡页当天同步显示 ✗；再点一次恢复（清除标记）；
   已放弃后再勾选即视为完成（✓）
2. 打卡页首次进入按目标类型打开视图：月目标打开月视图，
   周目标打开周视图（修复月目标第一页显示周视图的问题）

【追加更新】v1.5.5 放弃后任务栏隐藏 + 可撤回：
1. 点击"放弃"后，该打卡目标当天从任务栏消失（不再显示）
2. 放弃弹出"✅ 打卡已放弃"提示，限时（约 3 秒）可点撤销，
   撤销后恢复当天的打卡项显示与未完成状态（打卡栏同步清除 ✗）

【追加更新】v1.5.6 日历互通 + 长按拖拽排序：
1. 修复打卡月视图 bug：表头为一~日（周一起始），但前导空格按
   周日开头算法计算，导致周日开头的月份（如 2026 年 2/3 月）整体
   错位一列、日期与周几对不上（getDay 语义修正为周一索引）
2. 节假日接入手机系统日历（可开关）：
   - 接入 @capgo/capacitor-calendar v8（新增 READ_CALENDAR 权限）
   - 个人页新增「📅 节假日同步手机日历」开关：开启时请求只读授权，
     打卡/日历页的节假日实时读取系统日历（订阅的"中国节假日/假期"
     类日历事件，含跨天假期展开；调休补班事件自动排除）
   - 打卡周/月视图、日历月历、一周条、未来任务标题均显示合并后的
     节假日标记；未授权或手机无节假日订阅时自动回退内置节假日表
   - 查询按月缓存（10 分钟内不重复），事件标题自动清理年月前缀并
     匹配节日 emoji
3. 任务排序改为长按拖拽（移除每行 ▲▼ 按钮）：
   - 按住任务行约 1.5 秒触发拖拽（触发时轻微震动提示），行跟随手指
     实时移动，其它任务同步让位换序，拖到屏幕上下边缘自动滚动
   - 松手自动保存排序：普通任务写 Tasks 存储、打卡目标写 CheckIn
     存储（同一 order 空间，跨类型拖拽排序支持）
   - 手指提前移动>10px 视为滚动页面取消拖拽；拖拽结束短暂抑制
     click 防误触发；长按期间禁用系统文本选择/菜单

【追加更新】v1.5.7 拖拽排序修复（1.5.6 拖动不跟手）：
1. 修复长按 1.5 秒激活（震动）后行不随手移动：Android WebView
   在手指滑动时会判定为"滚动"并抢走触摸手势（触发 pointercancel
   结束拖拽），而 touch-action 必须在手势开始前生效、中途加类无效
2. 改为激活瞬间挂载非 passive 的 touchmove preventDefault 守卫，
   阻止浏览器接管滚动；配合 body overflow:hidden，拖拽全程行跟手，
   松手时统一移除守卫

【追加更新】v1.5.8 打卡日历状态精简（未推送，待下版一起推）：
1. 打卡日历状态从三种（蓝/灰底黑圈/半透明灰）精简为两种：
   - 成功：主色填充圆 + ✓（不变）
   - 失败：去掉灰色底，周视图显示醒目的红色 ✗；
     月视图日期正常显示、右上角加小 ✗ 标记
2. 移除"今天"的深色粗环（周/月视图）与未来日期的灰色淡化，
   未打卡日期统一为空白圆，不再被误认为独立状态

【追加更新】v1.5.9 拖拽改"把手"方案（未推送，待用户实测通过后一起推）：
1. 整行长按 1.5s 拖拽两版均失败：Android WebView 会把按住后滑动
   判为"滚动"抢走触摸手势（pointercancel），touch-action 中途
   改无效、post 激活 preventDefault 也无法阻止
2. 改为每行左侧 ⋮ 拖拽把手（touch-action:none）：按住把手即可
   直接拖动，浏览器自始至终不把手势判为滚动，指针事件流稳定；
   松开落位实时换序、保存逻辑不变；其余区域点击/滚动不受影响


【追加更新】v1.5.10 图标与顶部颜色修复（未推送，待用户实测通过后与 v1.5.7/8/9 一起推）：
1. 修复手机桌面显示安卓默认图标：旧的自适应图标（v1.2.0 已有猪猪）只覆盖
   Android 8+，旧版 Android 与部分启动器读取的 PNG 一直是初版模板默认图。
   - 用 public/pig.svg 生成全套 mipmap（mdpi~xxxhdpi）ic_launcher.png /
     ic_launcher_round.png（圆形裁切版）/ ic_launcher_foreground.png，
     背景 #FF9BB3、猪居中的方形图标
   - 自适应图标前景套 <group scale 0.58> 缩入安全区（66dp），避免圆形/
     方形遮罩裁掉耳朵；启动页 splash.png 也换成粉渐变底 + 猪
2. 修复顶部黑色/与页面颜色不统一：
   - 移除 MainActivity 的 FLAG_FULLSCREEN 与主题 windowFullscreen——
     之前强制隐藏状态栏，挖孔/刘海机型顶部会出现黑色 letterbox 条，
     Android 15+ 还会忽略全屏标记导致状态栏样式失控
   - 状态栏改为跟随 App 主题：浅色主题浅粉底 #FFF5F5 + 深色图标；
     酷黑主题 #12121A + 浅色图标；主题切换时由 @capacitor/status-bar
     运行时更新（新增依赖），Manifest 声明 opt-out 边到边以保持兼容


【追加更新】v1.5.11 修复 v1.5.10 状态栏遮住页面头部（未推送）：
1. 用户反馈顶部颜色已统一但仍遮住导航栏：Android 15/16 强制
   edge-to-edge（targetSdk 36 无法 opt-out），状态栏为透明、内容
   上顶。index.html 已有 viewport-fit=cover，Capacitor 8 进入
   "穿透"模式后需要应用自己用 env(safe-area-inset-top) 让位，
   而 App 从未做顶部安全区（以前全屏模式不需要）
2. 修复：.app-shell 增加 padding-top: env(safe-area-inset-top)，
   内容下移让出透明状态栏；空出的条带显示页面 --bg 背景，浅色/
   酷黑主题均自动统一；底部导航原有 env(safe-area-inset-bottom)
   兼容。API<15 设备 env 为 0 无副作用
3. 图标问题排查：v1.5.10 已替换全部 PNG/矢量图标资源（APK 内
   已验证），若桌面仍显示旧图标为启动器缓存，先重启手机刷新，
   无需卸载（卸载会丢本地数据）


【追加更新】v1.5.12 图标改用纯 PNG 自适应前景并整体改名（未推送）：
1. 小米手机上"应用管理/桌面"仍显示安卓默认兜底图标：系统设置直接读
   包资源（排除桌面缓存），判为加载自适应图标失败后回退默认图
2. 处理：自适应图标前景从矢量 drawable（aapt2 会拆成多文件，部分
   系统加载不稳）改为全密度 PNG；同时图标资源整体改名 ic_launcher2
   （legacy PNG + adaptive XML 同步），强制所有系统/启动器重读，
   绕开按旧资源 ID 的系统级图标缓存


【追加更新】v1.7.0 v2.0 第一批：个人页折叠 / 标签体系 / 记账偏好（未推送）：
1. 个人页折叠为两级：一级门面（头像占位 + 权限设置/个人中心入口卡），
   二级页 /profile/permissions（系统授权类）、/profile/center（个人中心）、
   /profile/tags（标签管理）
2. 统一标签表（pigbaby_tags_v2）：首次启动自动迁移内置+自定义标签与颜色；
   标签管理支持新增/重命名/换色/删除；重命名同步更新全部历史记录、
   颜色、旧备注计数器
3. 收入独立标签体系：收入默认标签 工资/红包/理财/报销/转账/其他；
   记账弹窗收入也有标签选择；记录列表收入显示标签；按日查询新增
   "当日收入分类"图；标签统计页加 支出/收入 切换；短信收入按关键词打标
4. 按日查询柱状图改为竖向（自下而上），标签在柱底、金额在柱顶
5. 打卡目标天数由 −/+ 改为直接输入数字（周 1-7 / 月 1-31，越界自动夹取）
6. 功能模块开关（个人中心）：都要 / 仅记账 / 仅任务，导航栏按开关隐藏
   对应标签（日历与个人固定保留）；日历从任务页解耦为独立组件，
   任务关闭时走独立日历页 /calendar
7. 中心小猪动作可配置（个人中心）：记支出 / 创建任务；对应模块关闭时
   自动落到另一个可用动作
8. 记账模式（个人中心）：预算制（本月剩余）与余额制（录入当前余额，
   显示"余额 + 本月支出"，月预算变为进度条）切换；存钱罐页余额语义跟随
9. 记账智能推荐（tagMemory 引擎）：从历史记录实时推导，输入备注推荐标签、
   按 标签+时段 推荐常用备注（✕ 改为忽略该条推荐）；个人中心可关闭；
   引擎供第二批自动记账打标复用


【追加更新】v1.8.0 v2.0 第二批：自动记账（通知监听，未推送）：
1. 原生层：NotificationListenerService 只监听微信/支付宝两个包的通知
   （其余应用不读取、不保存），采集标题/正文等文本存入原生队列
   （App 关闭期间也接收，保留 7 天 / 最多 300 条）；Capacitor 桥插件
   提供 授权状态检查 / 跳转系统设置 / 拉取与清除捕获，App 存活时实时推送
2. JS 层（autoCapture.ts）：解析通知文本（金额、收支方向、对方/商户）+
   跨通道去重（与已有记录、短信导入 ±10 分钟 + 金额 + 方向）+ 打标
   （商户关键词规则 → 记账记忆引擎 → 其他）；解析规则全部在前端，便于校准
3. 待确认流：捕获后默认生成"待确认"草稿，出现在记账页顶部卡片，
   支持逐笔 确认/点击修改后入账/忽略，以及"全部确认入账"；
   个人中心可开「跳过确认直接入账」（默认关）
4. 设置入口：权限设置页新增「自动记账（通知监听）」行（状态自检 +
   去开启 + 立即同步）；个人中心新增「自动记账」分组：总开关、
   跳过确认、AI 增强（预留）、最近捕获入口
5. 🔍 最近捕获（解析校准）页：显示原始通知文本与解析结果，
   未生成草稿时给出原因（未识别金额/方向），供真机校准正则
6. 说明：通知使用权只能由用户在系统设置中手动授予（代码无法申请）；
   本版安装后需真机做一笔小额支付来校准解析规则

【v1.8.0 修复】插件注册时机（versionCode 22，未推送）：
1. 真机反馈"自动捕捉插件在安卓不被允许"（插件未实现错误）
2. 根因：Capacitor 8 的 Bridge 在 BridgeActivity.super.onCreate() 内部创建
   并一次性注册插件列表，MainActivity 在 super.onCreate() 之后再调用
   registerPlugin() 不会生效。参见 Bridge.java：create() → 构造函数内
   registerAllPlugins() 遍历的是创建时的列表快照
3. 顺带发现：短信的 SmsReaderPlugin 一直以同样方式注册，实际从未生效
   （此前"点击授权短信直接失败、无弹窗"的症状正是插件未注册的表现，
   当时的权限流程改造没有解决根因）
4. 修复：两个本地插件改到 super.onCreate() 之前注册

【追加更新】v1.8.1 移除短信自动记账 + 补全通知文本采集（未推送）：
1. 按需求移除短信自动记账：删除前端 SmsAutoPanel/SmsAutoSync/smsAuto.ts
   与原生 SmsReceiver/SmsParser/SmsStore/SmsReaderPlugin；Manifest 移除
   短信接收器与 RECEIVE_SMS/READ_SMS 权限；MainActivity 去掉注册。
   标签关键词规则表迁至 src/tagRules.ts 供自动记账使用
2. 真机反馈"抓到了但不完整"（如"已扣费¥12.88"能解析，"退款到账通知"
   因正文无金额而失败）。原生采集扩展为读取通知里的全部文本：
   新增 summary/info/titleBig/conversationTitle，并兜底收集 extras 里
   其他所有 CharSequence（含多行样式 textLines），去重、限长（每段 160、
   总计 800 字）；解析与调试页改用全部文本拼接
3. 微信普通聊天通知预筛：不含金额/支付/到账等账单关键词的通知直接丢弃，
   避免队列与调试页被无关消息淹没（宁可多留、不可漏抓）
4. 「最近捕获」页：解析失败的原文不再被清除，保留供校准；新增"清空捕获"
   按钮；展示全部文本字段

【追加更新】v1.8.2 个人页功能行：简介折叠 + 开关按钮化（未推送）：
1. 新增通用组件 FeatureRow / ToggleButton：功能名右侧带 › 角标，点击功能名
   展开/收起简介（简介默认折叠）；开关类功能改为单个按钮——开启时点亮
   （主题色 + 光晕），再点一下关闭
2. 个人中心：智能推荐、自动记账总开关、跳过确认、AI 增强改为单按钮开关；
   各功能简介全部折叠；「当前余额」行保留"修改"按钮
3. 权限设置：自动记账行改为 状态 + 单按钮（未授权=去授权 / 已授权=立即同步）；
   通知权限、精确闹钟统一为同款行样式；节假日同步改为单按钮开关
4. 清理：删除短信面板遗留的 sms-auto-* 与旧 profile-row-* 样式

【追加更新】v1.8.3 通知采集深度补全 + 手动补记（未推送）：
1. 背景：真机反馈微信"扣费 4.04 + 退款 4.04"两条消息只抓到 1 条，且标签为"其他"。
   核查微信界面：扣费/退款是卡式凭证消息，商户名、金额等只存在于微信聊天卡片里；
   系统通知里只有一行"已扣费¥4.04"。另外退款那条在微信前台停留时微信不推送通知。
2. 原生采集再补全：递归读取嵌套 Bundle（MessageStyle 的 messages 消息体，
   含每条消息文本与发送人）、Person 姓名、tickerText；去掉群组摘要跳过；
   新增 rawDump 原始 extras 转储（key=value，仅供调试页查看，不参与解析）
3. 「最近捕获」页：新增"通知栏"与"原始字段"展示；解析失败的条目新增
   「手动补记这一笔」按钮（按文本猜收支方向，预填日期与来源，直接进记账弹窗）
4. 记忆引擎：排除"xx自动记账"这类通用备注，避免无商户的自动记录污染
   标签学习（例如把"微信自动记账"学成某个标签后误推荐给所有自动记录）

【追加更新】v1.8.4 捕获历史持久化 + 监听自检 + 通知栏扫描补抓（未推送）：
1. 问题：清后台后「最近捕获」里的数据消失。原因：该页读的是工作队列，
   成功解析的条目会被轮询清除（转成待确认草稿），失败的也会随队列变化；
   且原生写入用 apply()，被国产 ROM 杀进程时可能丢写入
2. 修复：新增 JS 侧持久捕获历史 pigbaby_capture_log（最近 120 条，记录每次
   捕获的处置结果：待确认/已入账/未识别原因/重复），不再随队列清除而消失；
   原生 NotifStore 改用 commit() 同步落盘，并按 (包名+时间+标题+正文) 去重
3. 监听自检：服务暴露连接状态，权限设置行显示「已授权·监听中 / 已授权·未连接」；
   描述补充国产系统省电策略指引
4. 补抓：新增 scanActive——打开 App/进调试页/每次轮询时扫描当前通知栏里
   微信/支付宝的通知，把"还在栏里但没被记录"的补进队列（覆盖监听服务
   短暂掉线期间的漏抓）
5. 「最近捕获」页改版：未处理队列（含手动补记）+ 历史记录两段展示

【追加更新】v1.8.5 无障碍读取账单（实验，未推送）：
1. 背景：微信在"你正在微信内"或会话免打扰时不推送通知，通知监听天然抓不到；
   用户选择探索无障碍方案
2. 原生 AccessibilityService（PigAccessibilityService）：accessibility_config.xml
   里仅订阅微信/支付宝两个包的事件；页面文字同时包含数字与账单类关键词
   （¥/账单/交易/退款/扣费/到账…）才保存整屏文本；1.2 秒节流 + 内容哈希去重；
   AccessStore 持久化（最多 50 条 / 3 天，commit 落盘）
3. 插件：checkAccessibility / openAccessibilitySettings / pullScreens / clearScreens
4. 「权限设置」新增「🧾 无障碍读取账单（实验）」行（状态 + 去开启）；
   「最近捕获」页新增无障碍抓屏区（原文展示 + 清空），用于拿到真实页面文本后
   编写提取规则
5. 定位：本版只做"被动读取"（不自动操作微信），先取真实账单页文本校准规则，
   下一步再实现自动提取入账

【追加更新】v1.9.0 多应用监听 + 消息整合过滤 + 学习标记 + 历史管理（未推送）：
1. 监听范围扩展：微信/支付宝 → 8 个应用（+ 抖音/淘宝/京东/拼多多/美团/云闪付），
   原生改为 包名→中文名 映射表；FINANCE_HINTS 补充 实付/已付/入账/退回/充值；
   NotifStore 容量 300→500 条、去重签名 120→240（消息量约 4 倍）
2. 消息整合（核心）：同一笔交易多个 App 各推一条通知时，按「同向 + 同额 + ±3 分钟」
   合并为一条草稿（补商户/备注/标签，日志记「已合并」），不再各自成账；
   金额提取改为候选扫描——优先"实付款/支付/扣款…"锚点后的数字，排除
   优惠/立减类数字（"共减3.00元，实付款55.00元" 正确取 55）
3. 转账敏感：微信/支付宝含「转账」的消息不自动生成草稿，保留在「最近捕获」
   并标红「转账·需手动补记」，可手动补记或忽略
4. 分类过滤：提醒类（待支付/待付款/待还款…，钱还没动）与广告物流类
   （物流/快递/发货/优惠券/限时/秒杀/签到/积分/开播…）自动过滤并记日志；
   其余走正常解析，解析不了的保留在队列标注「待判断」
5. 本地学习（AI 学习标记）：对不确定的消息点「✅ 以后记账 / 🚫 以后忽略」，
   从消息文本提取 ≤4 个特征词（去通用金融词）生成规则存本机；下次同类消息
   自动按规则处理（忽略的自动过滤、记账的直接解析出草稿）；
   可在「最近捕获」查看规则列表、命中次数并删除。全程离线，数据不出手机
6. 历史管理：历史记录每条可单独删除，顶部可一键清空（二次确认）
7. 交互修复：手动补记后会从捕获队列移除该条（此前补记完仍留在队列）；
   待确认卡片在"无草稿但有转账/待判断消息"时显示「N 条消息需手动处理」入口；
   App 回前台时补拉一次（后台 WebView 定时器被系统节流）；同步结果文案
   增加 合并/过滤/需手动处理 计数
8. 无障碍实验层与短信模块不变；仍为"采集在原生、解析/过滤/学习全在 JS"

【追加更新】v1.10.0 任务提醒日期修复 + 打卡✗同步 + 记账记忆自动生成 + 监听重连（未推送）：
1. 任务闹钟 bug 修复：提醒原来用完整日期+时间选择器，与任务日期互相独立，
   可能设到错误日期或已过去的时间导致不响。现在提醒只选「小时:分钟」，
   提醒日期自动跟随任务日期；调度时按「任务日期 + 时分」计算触发时间，
   旧任务的错误日期在下次刷新时自动纠正；时间已过时弹窗内直接提示
2. 打卡状态同步：打卡页此前只有主动「放弃」才显示 ✗，没记录的天是空白圆。
   现在目标创建日起、今天及以前、未完成的天直接显示浅色 ✗（与任务列表里
   当天的打卡项状态一致）；主动放弃仍是实心红 ✗，两者可区分；
   未来与相邻月的格子不受影响
3. 记账记忆生效：原来只有先打备注才会推荐标签，金额输入无任何联动。
   现在输入金额后自动按历史「时间 + 金额 + 标签词条」生成备注并填入
   （0.5 秒防抖；金额精确/±10%/±30% 分档、同时段加分、出现次数加权），
   同时显示「✨ 按金额时间推荐」候选可切换；手动改过的备注不会被覆盖；
   自动记账草稿同样生效（占位备注"XX自动记账"会被替换，商户备注保留）；
   编辑已保存的记录不会自动改写
4. 监听「已授权未连接」：权限设置页与「最近捕获」页新增「连接监听 / 重连监听」
   按钮，点按后请求系统重新绑定通知监听服务并刷新状态；仍失败时提示
   可关闭再重新开启通知使用权或重启手机

【追加更新】v1.10.1 重连交互修复（未推送）：
1. 问题：点「重连监听服务」没有任何可见反馈，且只等 1.5 秒——系统重绑是异步的
   （国产 ROM 可能要几秒），等待不足时界面与点按前完全相同，看着像"没反应"
2. 修复：重连改为轮询等待最多约 6 秒；按钮期间显示「连接中…」并在结束后
   明确提示结果（✅ 已重新连接 / 未连接成功及处理建议）
3. 权限设置与「最近捕获」页在未连接时都增加「系统设置」入口——部分 ROM 不响应
   requestRebind，需要在系统设置里把通知使用权关闭再重新打开才能恢复连接

【追加更新】v1.10.2 提醒日期可见化（未推送）：
1. 建/改任务弹窗里选完提醒时间后，直接显示「将在 X月X日 HH:mm 提醒」——
   明确可见提醒日期就是任务日期（v1.10.0 起的行为），改任务日期时提示同步变化
2. 无功能改动，纯交互确认

【追加更新】v1.10.3 处置结果回写最近捕获 + 教学回执（未推送）：
1. 记账页「待确认」卡片处理结果同步到「最近捕获」历史：确认入账/修改后入账
   记为「已入账」，忽略记为新状态「已忽略」（同 id 覆盖原「待确认」条目）
2. 「待判断」卡片点「以后记账 / 以后忽略」后给出明确回执：显示「处理中…」，
   结束后提示已记住（同类消息以后自动过滤/自动记账）；"以后记账"对解析不出
   金额的消息不会立即产生草稿，会明确提示可手动补记，不再像点了没反应
3. 无特征词的消息点教学按钮改为给出说明文案（此前按钮禁用、点了无任何反馈）

【追加更新】v1.10.4 个人区去掉文字旁小图标（未推送）：
1. 个人 / 个人中心 / 权限设置 / 标签管理 / 最近捕获 各页标题、功能行、按钮、
   弹窗标题前的装饰性 emoji 全部去掉（如「🔔 通知监听自动记账」→「通知监听自动记账」）
2. 头像占位的 🐷 保留（属头像，非文字装饰）；解析结果、教学按钮等表达状态的
   小符号（✅/⚠️ 等）保留

【追加更新】v1.11.0 本机昵称 / 头像自定义 + 云端登录代码就绪（未推送，云端未启用）：
1. 个人页用户卡片改为可点击：显示本机昵称与头像（默认 🐷），点开「编辑资料」弹窗——
   昵称输入（最多 16 字）+ 头像选择：从相册选图 / 12 个预设图案 / 恢复默认
2. 相册图片自动压缩为最长边 256px 的 JPEG（约 20~40KB）后存在本机设置里，不联网；
   头像是圆形裁切显示，重新选一张即可更换
3. 云端方案（邮箱验证码登录）代码已开发完成但暂不启用：CloudBase 环境按量计费，
   确定要推广时再开通。届时在控制台开通环境（邮箱验证码登录 + 发件邮箱 + 安全
   域名 localhost）并把环境 ID 填入 src/cloudbase.ts 即可启用，登录页 /login、
   个人页登录态、昵称云端保存等代码已备好
4. 说明：本机资料只在这台手机上生效，换设备不跟随（云端登录启用后可再接同步）
